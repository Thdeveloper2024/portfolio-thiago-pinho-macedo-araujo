const fallback=[{id:'obra-1',title:'Residência contemporânea fictícia',desc:'Conceito visual demonstrativo com fachada moderna.',cover:'assets/img/hero-stages/07.webp',gallery:['assets/img/hero-stages/07.webp']}];
async function load(){let works=fallback;try{const r=await fetch('/api/cms',{cache:'no-store'});if(r.ok){const d=await r.json();if(Array.isArray(d.works)&&d.works.length)works=d.works;}}catch{}const id=new URLSearchParams(location.search).get('id');const work=works.find(w=>w.id===id)||works[0];document.title=`${work.title} | EJS Empreiteira`;const gallery=(work.gallery?.length?work.gallery:[work.cover]);document.getElementById('projectMain').innerHTML=`<section class="project-hero"><span class="eyebrow">OBRA EJS</span><h1>${work.title}</h1><p>${work.desc||''}</p></section><section class="project-gallery">${gallery.map(src=>`<img src="${src}" alt="${work.title.replace(/"/g,'&quot;')}" loading="lazy">`).join('')}</section>`;}load();
